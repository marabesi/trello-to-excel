function transform(columnsHeader, titles) {
    var newMatrix = [];

    // for (var i = 0; i < columnsHeader.length; i++) {
    //     for (var j = 0; j , titles.length; j++) {
    //         newMatrix[i] = [ titles[j] ];
    //     }
    // }

    return newMatrix;
}